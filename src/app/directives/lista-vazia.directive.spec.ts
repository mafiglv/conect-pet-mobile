import { ListaVaziaDirective } from './lista-vazia.directive';

describe('ListaVaziaDirective', () => {
  it('should create an instance', () => {
    const directive = new ListaVaziaDirective();
    expect(directive).toBeTruthy();
  });
});
