import { FiltroPetsPipe } from './filtro-pets.pipe';

describe('FiltroPetsPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPetsPipe();
    expect(pipe).toBeTruthy();
  });
});
